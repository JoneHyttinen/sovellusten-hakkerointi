# h6 Onkohan tämä turvallinen käyttää? (Lari)

Suorittaakseni tehtävän, latasin ensin kaikki tarvittavat tiedostot ja paketit tehtävän tekoa varten seurasin tp-link-decrypt repositorion ohjeita.

Suoritin `./preinstall.sh` skriptin, suoritin `./extract_keys.sh` skriptin ja suoritin `make` komennon buildataakseni bin tiedoston.


